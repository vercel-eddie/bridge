{
  "id": "vercel-cli",
  "version": "1.2.0",
  "name": "Vercel CLI",
  "description": "Installs the Vercel CLI with automatic token passthrough from VERCEL_TOKEN environment variable or env file.",
  "options": {
    "version": {
      "type": "string",
      "description": "Vercel CLI version to install (e.g., 'latest', '50.8.1')",
      "default": "latest"
    },
    "envFileVar": {
      "type": "string",
      "description": "Name of environment variable containing the path to env file with VERCEL_TOKEN",
      "default": "VERCEL_ENV_FILE"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/node"
  ]
}
