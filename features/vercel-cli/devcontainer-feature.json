{
  "id": "vercel-cli",
  "version": "1.3.0",
  "name": "Vercel CLI",
  "description": "Installs the Vercel CLI with automatic token passthrough from env file.",
  "options": {
    "version": {
      "type": "string",
      "description": "Vercel CLI version to install (e.g., 'latest', '50.8.1')",
      "default": "latest"
    },
    "envFile": {
      "type": "string",
      "description": "Path to env file containing VERCEL_TOKEN (relative to workspace)",
      "default": ".env.development.local"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/node"
  ]
}
