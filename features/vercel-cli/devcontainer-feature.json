{
  "id": "vercel-cli",
  "version": "1.1.0",
  "name": "Vercel CLI",
  "description": "Installs the Vercel CLI with automatic token passthrough from VERCEL_TOKEN environment variable or env file.",
  "options": {
    "version": {
      "type": "string",
      "description": "Vercel CLI version to install (e.g., 'latest', '50.8.1')",
      "default": "latest"
    },
    "envFile": {
      "type": "string",
      "description": "Path to env file containing VERCEL_TOKEN (e.g., '/workspaces/myproject/.env.local')",
      "default": ".env.development.local"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/node"
  ]
}
